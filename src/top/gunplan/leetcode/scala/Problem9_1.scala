package top.gunplan.leetcode.scala

class Problem9_1 {

  private[scala] val Solution = null {
    def isPalindrome(x: Int) = {
      val b = x.toString
      if (b.reverse == b) true else false
    }
  }

}
